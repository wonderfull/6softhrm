Build both backend and frontend for production:
1. `npm --prefix backend run build` — compile TypeScript to dist/
2. `npm --prefix frontend run build` — Vite production build to dist/

Report success or any build errors (TypeScript type errors, missing imports, etc.).
