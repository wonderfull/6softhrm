Run a Prisma database migration:
1. `npm --prefix backend run prisma:migrate` — creates and applies the migration
2. `npm --prefix backend run prisma:generate` — regenerates the Prisma client

Report the migration name created and confirm the client was regenerated successfully.
