Run all tests for the project:
1. Run backend tests: `npm --prefix backend run test`
2. Run frontend tests: `npm --prefix frontend run test`

Report a summary of results for both, highlighting any failures with the test name and error message.
