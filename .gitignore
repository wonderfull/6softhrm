# Node & tooling
node_modules/
**/node_modules/
.env
.env.*
# …but the placeholder-only templates are tracked deliberately
!backend/.env.example
/dist
build/
.vscode/
/.DS_Store
/dev.db
**/dev.db
**/dev.db.backup
**/dev.db-*
coverage/
*.log
# local uploads
backend/uploads/
frontend/node_modules/
frontend/dist/
# Sensitive config files
RENDER_ENV.txt
*.zip
.htaccess
codexplans/
.superpowers/

# Claude Code — personal settings (contains machine-specific absolute paths)
.claude/settings.local.json
# Claude Code — personal instructions (not shared with team)
CLAUDE.local.md
# Hook debug output (temporary debugging tool)
hooks/hook-debug.json

# Browser automation session snapshots and scraped research data
.playwright-mcp/
tp-negative.json

# Browser screenshots from gate verification runs
kingfisher-dashboard.png

# Database backups (local copies)
backend/backups/

# Playwright run artifacts (traces, screenshots)
frontend/test-results/
frontend/playwright-report/
